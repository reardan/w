current state:

	build and verification...
		working:
			self-hosted x86 bootstrap:
				./w w.w -> bin/wv2 -> bin/wv3 -> bin/wv4 -> bin/wv5
			./wbuild verify:
				compares wv3 == wv4 == wv5
			./wbuild tests:
				runs compiler, grammar, lib, pointer, struct, range,
				data-structure, import, directory, threading, hello tests
			./wbuild update:
				archives the current seed and promotes the bin/wv3 fixpoint to ./w
				(local only; publishing a seed = release tag + SEEDS bump, docs/release.md)
			github releases:
				tag-triggered workflow publishes verified binaries for every
				target; bootstrap seeds download per the sha256 pins in ./SEEDS
			compiler CLI:
				-o output path
				usage message instead of segfaulting when no input is passed

	language and compiler features working:
		pointers:
			int*
			char*
			char* indexing
			char* dereference and assignment
			int* indexing
			& local address
			struct pointer fields
			arrays of structs via pointer indexing
		arrays, slices and strings:
			fixed local/global/struct arrays with descriptor headers
			T[] slices over typed buffers
			new T[n] heap arrays
			.length and .data pseudo-fields for buffers
			bounds traps for arrays, slices and strings
			array-to-pointer decay (T[N]/T[] pass their data pointer where T* or void* is expected)
			UTF-8 string descriptors with default "..." literals
			c"..." legacy C string literals
			UTF-8 decode/encode helpers and string codepoint iteration
			grapheme cluster counting and boundary helpers
		type table:
			type names carry size, pointer level, and field metadata
			pointer types are created on demand
			struct fields store field type and offset information
			int8, int16, int32 and unsigned integer types are registered
			bool, type aliases, const, enums, unions and typed fn pointers are registered
			x64 int64/uint64 arithmetic, fields, globals and returns are covered by tests
			types_compatible() checks pointer depth, base type and struct identity
			int is a word-sized scalar: int <-> pointer conversion requires cast()
			function values convert only to matching typed function pointers or via cast()
			assignment, initialization, call-argument and return mismatches warn
			function symbols record parameter types for call-site checks
			./wbuild warning_test asserts warning output against fixtures
			./wbuild self_host_warning_test keeps the compiler's own compile warning-free
		whitespace hygiene:
			warning when a line is indented with spaces instead of tabs
			warning when a file does not end with a newline
		UTF-8 source stage 1 (issue #287, docs/projects/utf8_source.md):
			a leading BOM is silently stripped
			check --json escapes invalid byte sequences so diagnostics
				stay valid UTF-8/JSON; columns count codepoints, not bytes
			UTF-8 identifiers (stage 2) remain a policy decision, not scheduled
		imports and packages:
			import a.b as f binds a file-scoped alias; f.name access is
				checked against the module's declarations (import_test)
			unqualified use of an alias-only module warns (warning_test)
			a path-shaped import spelling (import lib/assert.w) that fails
				to resolve echoes the line as written and hints the dotted
				form instead of the mangled search path; a '.*' wildcard
				suffix is rejected with a hint naming the plain import
				(import_diagnostic_test)
			package.wmeta metadata + wmeta checker validate modules and
				vendored path dependencies (./wbuild metadata_check,
				./wbuild metadata_test, design: docs/package_metadata.txt)
		struct:
			local struct values
			mixed-width struct fields
			struct pointers
			struct arrays
			new point() and new point allocate struct storage
			new point(1, 2, 3) initializes fields in declaration order
			struct values pass by value (stack copy) as function parameters
			struct values return by value and can feed field access / method receivers
		float:
			float/float32 arithmetic, comparisons, coercions, params and returns work on the default target
			float64 literals, arithmetic, formatting and returns work on x64
			x64 tests also cover float32 narrowing from a float64 literal
			float output is diff-tested against C reference output
			TestFloat-derived conformance vectors cover float32 (both
				targets) and float64 (x64) rounding/edge cases (issue #17,
				docs/projects/float_testing.md)
			float16 storage/conversion works on the x86 family (F16C
				hardware instructions; design: docs/projects/float.md)
			bfloat16 and float16 on arm64/wasm remain future work
		built-in map/set:
			map[K, V] and set[K] types
			new map/set allocation, typed literals, indexing, assignment and in-membership
			map and set .length pseudo-field
			m.remove(key), s.remove(key) and s.add(key) pseudo-methods
			m.get(key) (traps like m[key]) and m.get(key, default) pseudo-methods
			m.add(key) / m.add(key, delta) accumulation from zero (integer and float values; float64 on x64)
			m[k] op= v compound assignment (key evaluated once, missing key traps; the trap names the key)
			m.keys(), m.values() and s.keys() insertion-order list snapshots
			new map[K, V](value|factory) / new map[K, container]() opt-in missing-key defaults with auto-vivification (issue #327; docs/projects/map_default_factory.md)
			string keys compare by contents
			for-in iteration over map keys and set members
			for k, v in map two-variable iteration
			nested map keys ('m[h[k]]', any depth): the '[' handler keeps
				the element's slots in locals and commits the hash_index_*
				pending state only after ']', the ndarray-index discipline
				(found 2026-08 as a runtime crash, fixed the same month;
				grammar/postfix_expr.w, tests/map_nested_key_test.w)
		built-in list:
			list[T] typed lists with literals, indexing, push/pop and .length
			negative indexing counts from the end (l[-1] is the last element)
			l[i:j] copying slices with open and negative bounds (l[:j], l[i:], l[:])
			l.insert(i, v), l.remove(i) and l.clear() pseudo-methods
			x in l membership (content compare for char* elements)
			runtime traps say what trapped on stderr before exit(1) (index echoes index and length; pop/min/max on empty say so)
		type <=> json:
			to_json(struct) / from_json(T, value) compiler-generated codecs
			descriptors from the type table, runtime in structures/json_codec.w
			imported on demand (design: docs/projects/protocol_ergonomics.md)
			float fields (float32) and map[K, V] fields with char*/string keys
		expressions:
			&& and || short-circuit expressions
			! unary operator
			unary - and unary +
			++x / x++ / --x / x-- increment/decrement statements (#103):
				statement position only, sugar for += 1 / -= 1 (raw-byte
				pointer stepping like +=); expression use is an error
				(docs/projects/increment_decrement.md)
			unary operators bind tighter than binary and can stack (!!x, -*p, *&x)
			relational operators chain left-associatively
			hex literals
			string escape handling
			char literals: simple escapes plus \xNN, \uNNNN, \UNNNNNNNN and
			raw UTF-8 codepoints; unknown escapes are compile errors
			pointer-aware [] and . code generation
			explicit cast(type, expr) escape hatch for unsafe conversions
			mul_hi / mul_wide / add_carry 32-bit limb intrinsics (#213):
				unsigned 32x32 high half, wide product and carry-out add
				on the operands' low 32 bits; user symbols shadow them
				(limb_builtin_test, limb_builtin_64_test, arm64_smoke_test)
		control flow:
			for int i in range N
			range(end), range(start, end), range(start, end, step)
			range arguments can be expressions
			for x in <container> cursor-protocol iteration
				T_iter_begin/done/next/value over any struct pointer
				array_list, linked_list, hash_map (keys) implement it
				generic containers resolve the protocol as name_iter_*[arg]
				instantiations (one type parameter); heap[T] and deque[T]
				(structures/heap.w, structures/deque.w) implement it
			for int cp in string codepoint iteration
			break and continue
			nested if/loop behavior covered by tests
			if/while conditions work with or without parentheses
			{} blocks, dedent-terminated : blocks, and pass as a no-op
		functions:
			function arity warnings
			function pointer values can be called
			function pointer arguments are covered in grammar tests
		dynamic linking (both targets):
			c_lib "libc.so.6" registers a DT_NEEDED shared library
			extern int getpid() declares an imported C function
			extern int printf(char* fmt, ...) declares a variadic C function
			extern void* stdout imports a data object via a COPY relocation
			eager GLOB_DAT relocations fill inline GOT slots at load time
			generated shims convert W's stack convention to cdecl (x86)
			and System V registers (x64), 16-byte aligned
			float/double args and returns follow the platform ABI
			(xmm registers on x64, x87 st(0) returns on x86)
			variadic calls emit the ABI conversion inline per call site
			with C default argument promotions (float32 -> float64)
			./wbuild dynamic_test / dynamic_64_test call libc on both targets
			./wbuild float_abi_test / varargs_test / extern_data_test cover the ABI
			./wbuild cuda_smoke runs a PTX vector add through libcuda (x64)
		gpu (design: docs/projects/cuda.md; x64 Linux only):
			PTX emitter (code_generator/ptx.w): kernel bodies compile to
				PTX text embedded in the executable (__w_ptx_module,
				--ptx=<path> dump), no toolkit needed — the driver JIT
				compiles at cuModuleLoadData time
			'kernel name(params):' declarations with thread_idx/block_idx/
				block_dim/grid_dim intrinsics and int/pointer/float32/float64
				arithmetic, if/while/switch control flow
			'launch k[grid, block](args)' and 'gpu for i in range(n)' /
				range(start, end) (body outlined into a kernel, enclosing
				variables captured as parameters; scalar captures are
				read-only device copies); both async — gpu_sync() is the
				sync point
			atomic_add/atomic_min/atomic_max device intrinsics (int*;
				atomic_add also float32*), returning the pre-update value
				(atomic_add plus atomic_cas also lower host-side on
				x86/x64 with the same contract — see threading)
			the nine 32-bit limb/bit intrinsics work on device with the
				host contract (cuda_test cross-checks against the host)
			lib/cuda.w runtime: managed memory (gpu_alloc/gpu_free),
				explicit memory (gpu_device_alloc + gpu_memcpy_to/from,
				blocking copies ordered after launches), lazy driver
				init, kernel-handle cache, gpu_available() driver+device
				probe (non-fatal, the CPU-fallback branch point)
			lib/tensor.w (docs/projects/torch.md stages 2-3): GPU tensor on
				managed memory, elementwise ops/relu/atomic sum/naive
				matmul2, ndf round-trip copies, CPU fallback per op
			./wbuild gpu_ptx_emit_test / cuda_diagnostics_test /
				tensor_compile_test run GPU-less; ./wbuild cuda_test runs
				vector add + saxpy and tensor_gpu_test the lib.tensor
				surface (both paths) on a real GPU
		c_import:
			preprocesses C headers with macro expansion, conditionals and include resolution
			importer builds typedefs, structs/unions, enum values, macro integer constants and function prototypes (fixed-arity and variadic)
			extern data objects (stdout, environ, optind) import as weak COPY relocations
			extern arrays of known or unknown length (tzname, the sys_errlist shape) import by address as pointer globals (array decay through a loader-filled slot)
			old-style (K&R) declarations import as variadic zero-fixed-parameter functions; K&R definitions parse and are ignored like other header definitions
			broad libc/system headers are tested on both targets (stdio, stdlib, string, unistd, fcntl, errno, time, signal, ctype, math, dirent, locale, sys/stat); c_import_torture_test pins gnarly header shapes compile-only
			imported C struct layout is checked against kernel-filled struct stat
			bit-fields lay out per the SysV i386/x86-64 rules (storage units, straddle, ':0', unnamed, unions); sizeof and neighbor offsets pinned against gcc (x64_c_import_bitfield_test runs, c_import_bitfield_fixture compile-only); members read/write directly (sign-extending reads, read-modify-write stores, compound/increment forms) with gcc-pinned values; only an i386 long long field wider than the word keeps a dedicated compile error
		debugging information:
			debugger statement emits int3
			DWARF line-number data is emitted for gdb source-line mapping
			wdbg in-process debugger (./wbuild wdbg, ./wbuild debug_test, w --debug)
				x64 build too (./wbuild wdbg_x64, ./wbuild debug_test_x64): 64-bit
					sigcontext offsets and runtime signal thunks
				step/next/stepi/finish, breakpoints (function/line/file:line);
					an unresolved target suggests close-by names (edit distance)
				locals/args/globals by name, set, x, backtrace, list
				print compiles and runs W expressions in-process; locals and
					args visible at the stop are bound into the expression
				frame/up/down select a backtrace frame; print/set/x/info
					locals/args address the selected frame
				software watchpoints (statement-granularity single-step scan)
				conditional breakpoints, hit counts and logpoints
					(condition/ignore/log; docs/projects/debugger_conditional_breakpoints.md)
				fatal-signal post-mortem stops (segv handler)
				line editing and history at the prompt (lib/line_edit.w)
				wdbg --attach <pid> [file.w]: out-of-process ptrace attach --
					attach/detach/kill, registers/memory/stack (memory and register
					reads/writes share a seam with the in-process debugger,
					debugger/memory.w + debugger/registers.w), x86 and x86-64
					symbolization (recompiles the target for whichever word size
					the running debugger binary itself is, calibrates against
					/proc/<pid>/exe) enabling list/where/bt/i functions/i files,
					breakpoints and execution control (b/d/c/s/n/si/fin) --
					s/n drive PTRACE_SINGLESTEP to the next statement boundary
					the same way wdbg.w's in-process stepping does, fin runs to
					the caller's return via a temporary breakpoint from the
					frame walk -- frame selection (f/up/down) and locals/args
					(i locals/i args) through the same seams -- restricted
					expression eval (debugger/attach_eval.w: p/set/watch take
					fields, *deref, [index], unary -/& and + - * / %
					arithmetic, interpreted out-of-process through the memory
					seam; in-target calls rejected) -- hardware watchpoints
					(watch <expr|0xADDR>, DR0-DR3 write watches via
					PTRACE_PEEKUSER/POKEUSER, DR6 names the one that fired,
					4 max with a hard "no free debug register" error,
					d w/i w manage them);
					design: docs/projects/debugger_attach.md
			fatal-signal crash reports outside the debugger (lib/crash.w,
				issue #378): crash_handler_install() prints signal, pc,
				faulting address and a symbolized heuristic stack trace
				from the binary's own .symtab/.debug_line
				(lib/stack_trace.w), then re-raises the unchanged default
				signal death; installed by w.w (compiler crashes) and
				lib/testing.w (crashing tests), opt-in elsewhere,
				W_CRASH_TRACE=0 disables; Linux x86/x64 only
			core-dump processing (tools/wcore.w, ./wbuild wcore, issue
				#378's tooling half): bin/wcore [--json] <core> <binary>
				reads a Linux ET_CORE dump of a W x86/x86-64 binary (word
				size detected from the core's ELF class, one 64-bit build
				handles both), prints the signal and registers
				(NT_PRSTATUS; NT_SIGINFO adds si_code and the faulting
				address), the faulting pc symbolized to function
				(file:line) via the binary's own .symtab/.debug_line
				(lib/stack_trace.w's lookups pointed at the on-disk
				image) and a heuristic backtrace (the live tracer's
				return-address scan, reading stack words from the core's
				PT_LOAD segments); wcore_test crashes the null-deref
				fixture under ulimit -c on both word sizes and processes
				the real kernel cores, skipping cleanly when
				core_pattern is piped or cores cannot appear
		library and data structures:
			malloc/free/realloc as a library module (lib/memory.w)
			list
			hash_map
			string builder
			array_list
			linked_list
			format helpers
			command-line argument parsing (lib/args.w)
			buffered streams, line readers, Content-Length framing (lib/stream.w)
			whole-file helpers (lib/file.w, design: docs/projects/streams.md)
			Unix file metadata + process primitives (lib/stat.w: statx/chmod/
				utimensat/readlink/symlink wrappers, mode predicates;
				file_utimens/file_chown/file_lchown; lib/passwd.w: /etc/passwd
				+/etc/group, no NSS; process_wait_any for xargs -P-style pools;
				design: docs/projects/unix_primitives.md) with CLI tools
				(tools/{stat,chmod,touch,readlink}.w; darwin/win64/wasm stubs
				return -1)
			float32 math helpers (lib/fmath.w: fsqrt, fabs, bit casts)
			float32 transcendentals (lib/fmath.w: exp/log/pow/trig family,
				measured ulp bounds, tested against glibc goldens on both targets)
			float64 math helpers (lib/fmath64.w: full fmath surface ported to
				float64 for 64-bit-word targets, incl. exp/log/pow/trig
				transcendentals with measured ulp bounds tested against
				glibc goldens; docs/projects/engineering_math_baseline.md)
			seeded deterministic PRNG (lib/rand.w: xorshift32, uniform
				(rand_below, rand_float) and gaussian (rand_gaussian, Box-Muller)
				sampling, bit-identical across targets)
			descriptive statistics (lib/stats.w: accumulator, variance,
				quantiles; design: docs/projects/stats.md)
			dense multi-dimensional arrays (lib/ndarray.w/ndarray64.w v1:
				ndf/ndi/ndf64, rank 1-4, matmul on 2-D, m[i, j] index
				sugar lowering to the accessors; freeing via the
				slice-level array_free (lib/array.w) and parallel_for
				twins (lib/ndarray_par.w, Linux x86/x64);
				design/status: docs/projects/ndarray.md, issue #27)
			shell helpers (lib/shell.w: sh(cmd), run(argv), cd, env get/set
				on top of lib/process.w/lib/env.w)
			compression (libs/extras/compress/: CRC-32, Adler-32, a
				conformant DEFLATE inflater (stored/fixed/dynamic Huffman),
				gzip/zlib wrapping; full encoder: LZ77 + fixed/dynamic
				Huffman with a per-block stored fallback bounding
				worst-case expansion, cross-validated against real zlib
				(compress_zlib_interop_test, optional/python3-gated);
				design: docs/projects/compress.md)
		repl:
			./bin/wv2 repl.w -o bin/repl && ./bin/repl builds and runs repl.w
			./wbuild repl_test verifies REPL interaction and error recovery
			entries compile into an executable mmap buffer and run immediately
			multi-line entries (functions, structs, control flow), Python-style:
				':' opens a block ended by a blank line, brackets keep reading
			interactive auto-indent (tty only): ':' indents the next line,
				return/break/continue/pass dedent, a blank line dedents one
				level and ends the entry at column 0
			top-level declarations persist between entries (storage in the buffer)
			redefinition shadows the old symbol (Python-style rebinding) and
				repatches already-compiled call sites to the new binding
				(late binding, issue #114, repl_apply_late_bind in repl/core.w)
			bare expressions echo their value by compile-time type
			imports and extern/c_lib declarations work at the prompt
			./bin/repl file.w [args...] runs a file, then attaches the prompt
				to its live definitions (--no_main skips running main)
			compile errors roll back state and return to the prompt
			line editing and history on a tty (lib/termios.w raw mode +
				lib/line_edit.w, history persisted to ~/.w_history)
			x64 repl (./bin/wv2 x64 repl.w -o bin/repl64; ./wbuild repl_test_x64): word-size-aware
				buffer (MAP_32BIT) and real x64 repl_setjmp/longjmp stubs
			colon-commands: :symbols, :type expr, :time expr, :load file,
				:reset, :save file (issue #276)
			scripted/agent mode: -e "entry" one-shot, --json NDJSON per
				entry, banner/prompts move to stderr off-tty (issue #276)
			'!' shell escape: !cmd runs through lib/shell with inherited
				stdio, !cd/!export intercepted (issue #276)
			shell mode stages 1-4 (issue #335, design:
				docs/projects/repl_shell_mode.md): ':sh' toggles a mode
				where typed lines parse as shell commands and translate
				(repl/shell_translate.w) to native lib/shell_commands.w calls
				(pwd, ls incl. -l long listings, cat, echo, head, tail, wc,
				mkdir, rm, cp, mv, touch, chmod octal, du, ln -s, df, ps,
				grep via lib/regex.w) or farm out to a real binary; the
				metachar fallback scan is quote-aware ('*' etc. inside
				quotes translate, like sh treats them); remaining stages
				(pipes, find/sed, hard-link ln, standalone wsh) are staged
				in the design doc

	vcs (libs/extras/vcs/, tools/wvc.w; design: docs/projects/version_control.md, issue #252):
		working:
			content-addressed object store (cas.w), diff (diff.w), commit
				DAG + merge-base (dag.w), working trees (tree.w),
				commits (commit.w)
			dirstate index with a (size, mtime) stat cache for O(changed)
				status (index.w)
			binary deltas: rolling-hash block table, copy/insert opcodes,
				bounded-depth chains (delta.w)
			three-way merge over diff.w hunks with conflict markers
				(merge3.w)
			wvc porcelain: status/snapshot/merge/serve/pull/push; push/pull
				do have/want negotiation for loose objects over HTTP
			object packing (pack.w): loose objects repacked into one
				deflate-compressed, indexed W-native .wpack file (wvc pack
				[--prune] / wvc unpack); reads come back through cas.w's
				fallback seam, so status/log/pull work against packed stores
			pack-internal re-deltification (wpack 2): sliding-window
				cross-object delta compression inside a pack, bounded-depth
				same-pack base chains; wpack 1 files stay readable
		remaining:
			git interop (non-goal for now; needs zlib + SHA-1 + pack format)

	parser generator (libs/extras/parser_generator/; design: docs/projects/parser_generator.md, issue #329):
		working:
			grammar_reader.w/lexer.w/token_stream.w parse a .pg grammar file
				into an in-memory grammar_model.w
			generator.w emits an ordinary W module (no compiler/grammar/
				code_generator changes; ParserGenerator lives entirely under
				libs/extras/ as optional tooling)
			milestone 1: table-driven rule selection
			milestone 2: LL(1) analysis with committed dispatch for
				colliding FIRST tokens (analysis.w)
			milestone 3: optional streaming mode (parse without retaining
				the whole token stream), gated on milestone 2's analysis
				proving no backtracking is needed
			milestone 4: `{ code }` action blocks and `&{ expr }`
				predicates, commit-time execution, streaming-mode only
				(shipped 2026-07; analysis.w action/predicate units,
				tests/parser_generator/actions_sample.pg +
				generated_actions_test.w)
			c_import's header parser is generated by this tool

	current limitations:
		pointers and structs:
			new initializes fields from arguments but does not run user constructors
			struct values with fixed-array fields are copied and returned by value
		debugging:
			backtrace is heuristic (return-address scan, no frame pointers),
				so an older frame's base can be unknown (locals unaddressable)
			watchpoints are software (statement-granularity single-step
				scan): large slowdown while any exist
		threading:
			lib/thread.w (spawn/join/parallel_for with join-time stack
				reclamation, wmutex/wcond, host atomic_add/atomic_cas
				intrinsics) is Linux x86/x64 only; arm64/darwin/win64/
				wasm ports and thread pools are staged in
				docs/projects/threads.md
		webassembly:
			json builtins, generators, c_import/extern FFI, REPL and wdbg
				are not supported on the wasm target
				(docs/projects/wasm_backend.md)

	next priorities:
		type system:
			type.fields usage for globals and imports: verified complete
				2026-08 -- struct-typed globals (own and imported,
				nested/array fields, copies, by-value args) and
				c_import/extern types all flow through the type table's
				field metadata on both word sizes
			use type.w as the language meta-type
			support private imports (aliases + the unqualified-use warning
				are done; hard hiding needs real module identity)
			qualified type names through import aliases (f.SomeStruct):
				done 2026-08 -- resolved in type positions (locals,
				globals, params, returns, fields, pointers, casts, 'new',
				struct-constructor literals) with membership checked
				against the aliased module like the value spelling;
				import_alias_type_test / import_alias_type_error_test
			type_table migrated to list[int] (issue #96/#119); symbol_table
				stays a hand-packed byte blob on purpose (table offsets are
				stable handles threaded through backpatch chains, GOT slots,
				and debug-location bookkeeping — not a hash_map call site to
				migrate, see docs/projects/typed_containers.md)
		c_import:
			multi-load access for i386 long long bit-fields wider than the
				word (the one remaining bit-field access limitation; a
				dedicated compile error today)
			more system-header torture fixtures as regressions are found
				(c_import_torture_test is the home for them)
		debugger:
			wdbg disassembly view (done: `disas`/`disassemble` command in
				both wdbg and attach mode, debugger/disas.w, wired to the
				x86/x64/arm64 assembler/disassembler libraries — issues
				#163-#169/#171, docs/projects/assembler_disassembler.md)
			wdbg --attach #123 remainder: done 2026-07-28 -- restricted
				expression eval (debugger/attach_eval.w) and hardware
				watchpoints (DR0-DR3) both landed; in-target calls
				(gdb-style inferior calls) and dynamic/PIE targets stay
				out of scope (docs/projects/debugger_attach.md, issue
				#123)
		code generation:
			webassembly stage 5 residue: wasm64, real-signature
				exports, accumulator globals-vs-locals measurement
				(stages 0-4 plus the stage-5 json builtins, direct-call
				optimization, browser host shim, and '# wbuild:'
				wasm arch support — arch=/arch_only=/group=@wasm
				generate the wasm targets now — landed:
				docs/projects/wasm_backend.md)
			W^X text/data split: done 2026-08 — every file target now
				emits R+X text / R+W data (win64 stage A landed July
				2026 for the HVCI IAT crash; x64 stage B and x86 stage C
				landed 2026-08 with the extern-data/COPY move and the
				elf_wx_segment_test structural gate; arm64/arm64_darwin
				were already split). Residue: manual real-Windows HVCI
				smoke test, optional arm64 extern-data lift
				(docs/projects/wx_split.md)
		directory and build cleanup:
			import grammar vs grammar.* -- settled 2026-07: they always
				compiled exactly the same file (grammar.w) into the one
				global symbol table, so the '.*' suffix was a silent
				no-op that only implied a namespacing distinction the
				language does not have (aliases via 'as' are the
				namespacing feature). The reference grammar
				(tests/parser_generator/w.pg) never accepted '.*' and no
				tracked source used it, so the compiler now rejects it
				with a hint naming the plain form, keeping the spelling
				free for a real future meaning (selective imports).
				'import grammar' stays the one blessed spelling
				(grammar/import_statement.w, import_diagnostic_test)
			Makefile removed; ./wbuild + build.json is the single build entry
				point (migration record: docs/projects/wexec.md)

	language features (landed):
		generators + yield (docs/projects/iteration.md, stackful coroutines, x86 + x64)
		template strings: f"..." with embedded expressions (docs/projects/template_strings.md)
		variable arguments: T... slices (docs/projects/default_args_variadics.md)
		default parameter values (docs/projects/default_args_variadics.md)
		dynamic typing / var (docs/projects/dynamic_var.md)
		generics/templates with explicit instantiation (docs/projects/generics.md)
		generic type-argument inference at call sites (docs/projects/generics.md)

	future language features:
		generic inference for forward calls, struct constructors and container shapes (pair[T]*, list[T])
		inline asm functions
		bfloat16; float16 on arm64/wasm (design: docs/projects/float.md)
		type <=> json: float64 fields (needs float64 numbers in structures/json.w; float32 and map fields work)
		type <=> protobuf
		global default values

	future data structures:
		dict (done: built-in map iterates in insertion order, issue #145; defaultdict-style opt-in defaults with auto-vivification, issue #327)
		matrix / multi-dimensional arrays (v1 done: lib/ndarray.w/
			ndarray64.w library substrate, issue #27; grammar-level
			indexing sugar m[i, j] done: comma index lists lower to the
			atN/setN accessors, grammar/ndarray_index.w; design:
			docs/projects/ndarray.md)
		statistics (done: lib/stats.w, design: docs/projects/stats.md)

	future platforms:
		windows
		arm
		gpu (kernels/'gpu for'/atomics/explicit memory work — see the
			gpu inventory above; remaining: A2 virtual-register emission,
			shared memory, recoverable CUresult errors, multi-GPU)

	net:
		working:
			basic socket/connect/server paths (direct socket syscalls on
				every target incl. i386, Linux 4.3+; the socketcall(2)
				multiplexer is retired from lib/__arch__/x86)
			udp sendto/recvfrom helpers
			poll, nonblocking sockets, monotonic clock and sleep_ms
			Content-Length framing (lib/framing.w)
			JSON-RPC 2.0 servers, blocking or event-loop driven (lib/json_rpc.w)
			poll-based event loop with timers and cancellation (lib/event_loop.w)
			http client (libs/standard/web/http_client.w)
			http/https server framework (libs/standard/web/connection.w +
				http_server.w: ServerContext accept loop, RequestContext/
				routing, keep-alive, HTTPS via tls_accept; issue #235)

	web ui:
		editor
		file tree explorer
		search
		code editor
		binary analyzer
		parse tree analyzer
		task runner
			shell execute syscall
			fork
		debugger information
		w debugger

