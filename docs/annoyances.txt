DONE! -1 cannot be used, must use 0-1 instead.
DONE! cannot use # on end of line comments
DONE! compiler supports -o output path
DONE! break/continue
DONE! basic repl
DONE! free/realloc
DONE! function arity warnings
DONE! assignment pointer-level mismatch warning
DONE! deeper type compatibility checks (assignment, init, arguments, return)
DONE! nested ifs covered by tests
DONE! new type() allocation
DONE! new type(args) field initialization
DONE! struct values pass by value
DONE! for range(start, end, step)
DONE! repl compile errors return to the prompt instead of exiting
DONE! {} blocks work, dedent ends a scope, and pass is an explicit no-op
DONE! if/while work without () when using :
DONE! warning if file doesn't end in a newline
DONE! warning if spaces used instead of tabs

DONE! command arg parsing helper (lib/args.w)
DONE! malloc/free/realloc live in lib/memory.w as a swappable library module

no built-in w --debug debugger
no for loop list iteration (design brainstorm: docs/projects/iteration.md)
no generators
