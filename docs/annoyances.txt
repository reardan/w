DONE! -1 cannot be used, must use 0-1 instead.
DONE! cannot use # on end of line comments
DONE! compiler supports -o output path
DONE! break/continue
DONE! basic repl
DONE! free/realloc
DONE! function arity warnings
DONE! assignment pointer-level mismatch warning
DONE! nested ifs covered by tests
DONE! new type() allocation
DONE! for range(start, end, step)

still need {} instead of pass
	why is pass even required?? newline should end scope
if/while required () when using :
pointer and struct edge cases still need more coverage
typing still needs deeper compatibility checks
repl compile errors exit the process
no built-in w --debug debugger
malloc/free/realloc are still compiler/runtime primitives, not a library abstraction
no command arg parsing helper
no warning if file doesn't end in space
no warning if spaces used instead of tabs
no for loop list iteration
no generators
