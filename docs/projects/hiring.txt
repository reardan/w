Offloading Engineering


Required Knowledge
	Compiler Theory


Project Knowledge
	Assembly
		x86
		x64
		ARM
		WebAssembly
		gpu
	Testing
	Debugging
	Networking


Project Acceptance Tests
	make tests MUST PASS
	make [project_name]_test MUST PASS
	manual testing MUST PASS


Projects
	Types (partial)
		type table has sizes, pointer levels, fields
		remaining: deeper compatibility checks, type metadata imports
	Pointers (mostly implemented)
		remaining: by-value structs, constructor arguments, more edge coverage
	For..In..Range (implemented)
		range(end), range(start, end), range(start, end, step)
	x64 MVP (partial)
		hello-world/syscall path works
		remaining: lib_test and self-compiling
	WebAssembly MVP
	Signal Handling


Data Structure
	LinkedArray / array_list (implemented)
	HashMap (implemented)
	ParallelList
	ParallelMap


Secondary Priority Projects
	GPUAssembly
	ARM
	Windows
	Android
	Debugging
	Assembler
	Disassembler
	Networking Improvements
		Network socketcall
			Deopendant on struct*
		UDP
		HTTP
			Server
			Client

