
web ui

current state:
	repl:
		./bin/wv2 repl.w -o bin/repl builds a terminal REPL
		./wbuild repl_test verifies interaction and compile-error recovery
		multi-line entries: functions, structs, imports at the prompt
		top-level declarations persist between entries
		bare expressions echo their value
		repl file.w runs a file, then attaches the prompt
		compile errors return to the prompt
	debugging:
		DWARF line information is emitted for gdb
		no built-in debugger server or web debugger yet
	whttp:
		basic server experiments exist under tests/

future:
	editor
		CodeMirror Library
		send every keystroke
	debugger
		built on top of editor
	visualizer
		Visualizer
	repl
	terminal



backend server:
	whttp
		dict
		file watching
		reverse proxy
	websockets
		code server
		repl server
		command server
