
web ui

current state:
	repl:
		make repl builds a terminal REPL
		make repl_test verifies a simple interaction
		compile errors still exit the process
	debugging:
		DWARF line information is emitted for gdb
		no built-in debugger server or web debugger yet
	whttp:
		basic server experiments exist under tests/

future:
	editor
		CodeMirror Library
		send every keystroke
	debugger
		built on top of editor
	visualizer
		Visualizer
	repl
	terminal



backend server:
	whttp
		dict
		file watching
		reverse proxy
	websockets
		code server
		repl server
		command server
