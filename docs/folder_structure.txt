I'd like to migrate the code base to a tree structure.

Current state:
	The codebase already has compiler/, grammar/, code_generator/, lib/,
	structures/, tests/, and debugger/ directories.  The remaining cleanup is
	to settle import semantics for cases like grammar.w vs grammar/* and then
	split the build manifest (build.json) once the structure stops moving.

The local structure on a per-project basis would be as follows:


/
	w
		owners
		w.w
			compiler
				compiler.w
				grammar
					grammar.w
					program.w
					statement.w
					expression.w
					identifier.w
				symbol
				token
				codegen
			type
				...specified in types.txt





Global structure:

/
	owners admin:w
	w
		owners w,?
	bin
		TBD
	users
		w
			owners admin:w
			private
				owners w,private
		otheruser
			owners admin:otheruser
			private
				owners otheruser,private
		... 100 billion users
	shared
		owners admin:w
	experimental
		owners *, admin:w

