

Compiler
	Done
		Self-hosted x86 bootstrap
		Fixpoint verification with make verify
		-o output path
		Usage message for missing input
		Type table with size, pointer level, and struct fields
		Pointer-aware &, *, [], and struct field access
		Fixed local/global/struct arrays, slices, heap arrays and UTF-8 string descriptors
		Default "..." literals as UTF-8 string descriptors, c"..." for C strings
		UTF-8 decode/encode helpers, string codepoint iteration and grapheme helpers
		new type() allocation
		constructor-style new type(args)
		struct values passed by value
		range loops with optional start/end/step
		break and continue
		C-style unary precedence and relational chaining
		Basic REPL
		REPL compile-error recovery
		REPL multi-line entries, persistent definitions, expression echo
		REPL run-a-file-then-attach mode (repl file.w)
		DWARF line information
		Deeper type compatibility checks
		Explicit casts as the unsafe-conversion escape hatch
		Bool, const, type aliases, typed function pointers, enums and unions
		Struct return-by-value
		Built-in map and set syntax/runtime
		Built-in typed list[T] with struct elements and struct map values
		Float32/float64 support (float64 x64-only)
		X64 int64/uint64 support
		C header import for typedefs, aggregates, enums, macro constants and prototypes (fixed-arity and variadic)
		C interop: varargs calls, extern data objects (COPY relocations) and float/double ABI
		C preprocessor for macro expansion, conditionals and include resolution
		Broad libc/system-header import on x86 and x64
		Built-in debugger
		AI tooling MVP: w check --json, changed-file target mapper and w-toolchain MCP
		LSP MVP: wlsp with check-driven diagnostics and symbols-driven go-to-definition

		True generics/templates with explicit instantiation (monomorphizing re-parse)
		Generic type-argument inference at call sites (max(3, 5) infers T = int)
		Generators / yield (stackful coroutines)
		Template strings, default parameter values, W variadics, dynamic var

	Remaining
		Generic inference for forward calls, struct constructors and container shapes
		Richer container ergonomics (map get/default/discard/clear, contextual bare literals, struct keys)
		Float16 / bfloat16
		C import old-style declarations and bit-field layout fidelity
		Formatter, richer LSP features (locals, references, hover), semantic indexer and richer MCP tooling

Net
	Done
		Basic sockets
		Client connect path
		Server bind/listen/accept path

	Remaining
		UDP helpers
		HTTP client
		Socketcall cleanup

Platform
	Done
		Linux
		x86
		x64 (lib_test + self-compiling fixpoint, make tests_x64)

	Remaining
		WebAssembly
